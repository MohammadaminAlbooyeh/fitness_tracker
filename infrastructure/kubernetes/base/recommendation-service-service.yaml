apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: ecommerce
  labels:
    app: recommendation-service
spec:
  selector:
    app: recommendation-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
