apiVersion: v1
kind: Service
metadata:
  name: behavior-tracking-service
  namespace: ecommerce
  labels:
    app: behavior-tracking-service
spec:
  selector:
    app: behavior-tracking-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
