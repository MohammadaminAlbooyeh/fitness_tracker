apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: ecommerce
  labels:
    app: search-service
spec:
  selector:
    app: search-service
  ports:
    - port: 8083
      targetPort: 8083
  type: ClusterIP
