apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: ecommerce
  labels:
    app: analytics-service
spec:
  selector:
    app: analytics-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
