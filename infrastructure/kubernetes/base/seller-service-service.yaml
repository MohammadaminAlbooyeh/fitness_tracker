apiVersion: v1
kind: Service
metadata:
  name: seller-service
  namespace: ecommerce
  labels:
    app: seller-service
spec:
  selector:
    app: seller-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
